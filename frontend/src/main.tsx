import React from 'react'
import ReactDOM from 'react-dom/client'
import App_newest from './App_newest.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App_newest />
  </React.StrictMode>,
)
